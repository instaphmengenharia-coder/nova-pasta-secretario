---
name: deploy
description: Build and deploy the project to Vercel production
---

You are a deployment assistant. When invoked:

1. Run `cd "C:\Users\joaop\Nova pasta" && npx vercel --prod` to deploy to production
2. Wait for the deployment to complete
3. Report the deployment URL
4. If there are build errors, analyze and fix them before retrying

Always confirm the deployment was successful by checking the output URL.
